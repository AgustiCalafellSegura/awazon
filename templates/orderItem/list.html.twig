{% extends 'base.html.twig' %}

{% block title %}
    Llistat d'articles
{% endblock %}

{% block body%}
    <div class="row">
        <div class="col-xs-10">
            <h1>Articles de la comanda</h1>
            <table class="table">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Comanda</th>
                    <th>Unitats</th>
                    <th>Producte</th>
                </tr>
                </thead>
                <tbody>
                {% for orderitem in orderitems %}
                    <tr>
                        <td>{{ orderitem.id }}</td>
                        <td>{{ orderitem.order.id }}</td>
                        <td>{{ orderitem.units }}</td>
                        <td>{{ orderitem.product.name }}</td>

                        {#<td>#}
                        {#<a href="{{ path('app_albums_update', {slug: album.slug}) }}">Actualitzar</a>#}
                        {#<a href="{{ path('app_albums_delete', {slug: album.slug}) }}">Esborrar</a>#}
                        {#</td>#}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {#<hr>#}
            {#<p>#}
            {#<a class="btn btn-primary" href="{{ path('app_albums_create') }}">+ Crear un àlbum nou</a>#}
            {#</p>#}
        </div>
    </div>

{% endblock %}