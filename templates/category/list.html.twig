{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
        <div class="col-xs-10">
            <h1>Category</h1>
            <table class="table">
                <thead>
                <tr>
                    <th>id</th>
                    <th>name</th>
                    <th>actions</th>
                </tr>
                </thead>
                <tbody>
                {% for category in categories %}
                    <tr>
                        <td>{{ category.id }}</td>
                        <td>{{ category.name }}</td>
                        <td>
                         {#   <a href="{{ path('app_category_update', {id: category.id}) }}">Actualitzar</a>
                            <a href="{{ path('app_category_delete', {id: category.id}) }}">Esborrar</a>#}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            <hr>
            <p>
                {#<a href="{{ path('app_category_create') }}">+ Crear una categoria nova</a>#}
            </p>

        </div>
    </div>
{% endblock %}